<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE document PUBLIC "-//APACHE//DTD Documentation V2.0//EN"
"http://forrest.apache.org/dtd/document-v20.dtd">
<document xml:lang="en">
	<header>
		<title>Perl::Twig</title>
	</header>

	<body>
		<section>
			<title>Perl::Twig</title>

			<p><a href="http://www.xmltwig.com/xmltwig">Perl::Twig</a> is perl
				module that reads and writes xml. convert2xml.pl depends on it.
			</p>

			<ol>
				<li>To install it on OS X, issue the command:
				<code>sudo port install p5-xml-twig</code></li>
				<li>To install it on Linux, use on of these commands
					<code>sudo apt-get install libxml-twig-perl</code>,
					<code>sudo zypper install perl-XML-Twig</code> or
					<code>sudo yum install perl-XML-Twig</code>
				</li>
			</ol>
		</section>

	</body>
</document>
