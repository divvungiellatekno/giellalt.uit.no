<?xml version = '1.0' encoding = 'UTF-8'?>
<!DOCTYPE document PUBLIC "-//APACHE//DTD Documentation V2.0//EN" "http://forrest.apache.org/dtd/document-v20.dtd">
<document xml:lang="en">
  <header>
    <title>Special emacs modes</title>
    <authors>
      <person email="trond.trosterud@hum.uit.no" name="Trond Trosterud" />
    </authors>
  </header>
  <body>
    <section>
      <title>Special Emacs modes</title>
      <p>The Emacs mode definitions are located in project directory
      <code>$GTHOME/tools/emacs</code> under svn. Copy the relevant files
      to your <code>emacs</code> directory in your home directory (or better:
      use a direct path). Load the file by adding the command to your
      <code>.emacs</code> file: </p>
      <ul>
        <li><source>(load &quot;$GTHOME/tools/emacs/file-name&quot;)</source></li>
      </ul>
      <section>
        <title>correct-corpus-mode</title>
        <p>The file containing the mode definitions is <code>correct.el</code>. Command to add to your <code>.emacs</code> file:</p>
        <source>(load &quot;$GTHOME/tools/emacs/correct&quot;)
</source>
<p>Start the mode by typing</p>
        <source>M-x correct-corpus-mode
</source>
        <p>The keystrokes are:</p>
        <dl>
          <dt>C-c f</dt>
          <dd>Looks for the next cohort with several readings</dd>
          <dt>C-c c</dt>
          <dd>Adds a correct tag and proceeds to the next cohort</dd>
          <dt>C-c x</dt>
          <dd>Adds a correct tag and proceeds to the next line in the same cohort</dd>
          <dt>C-c t</dt>
          <dd>Passes by already marked cohorts (not fully working)</dd>
        </dl>
      </section>

      <section>
        <title>namelex-mode</title>
        <p>The file containing the mode definitions is <code>namelex.el</code>. Command to add to your <code>.emacs</code> file:</p>
        <source>(load &quot;$GTHOME/tools/emacs/namelex&quot;)
</source>
<p>Start the mode by typing</p>
        <source>M-x namelex-mode
</source>
        <p>The keystrokes are:</p>
        <dl>
          <dt>a</dt>
          <dd>adds tag -ani </dd>
          <dt>b</dt>
          <dd>-obj </dd>
          <dt>f</dt>
          <dd>-fem</dd>
          <dt>m</dt>
          <dd>-mal</dd>
          <dt>n</dt>
          <dd>For (skipping and) searching the next NAME </dd>
          <dt>o</dt>
          <dd>-org </dd>
          <dt>p</dt>
          <dd>-plc </dd>
          <dt>r</dt>
          <dd>-surplc </dd>
          <dt>s</dt>
          <dd>-sur </dd>
          <dt>t</dt>
          <dd>-tit </dd>
          <dt>u</dt>
          <dd>-surmal </dd>
        </dl>
<p>Note that when using the namelex-mode, its difficult to do
anything else (since the characters are reserved for tagging). Change
the mode for example to text-mode to do some other editing than
tagging and then back to namelex-mode for tagging.</p>
      </section>

      <section>
        <title>gender-assign-mode</title>
        <p>The file containing the mode definitions is <code>genderassign.el</code>. The purpose of the 
mode is to make it easier to assign gender to lexicon files.
	</p>
<p>Command to add to your <code>.emacs</code> file:</p>
        <source>(load &quot;$GTHOME/tools/emacs/genderassign&quot;)
</source>
<p>Start the mode by typing</p>
        <source>M-x genderassign-mode
</source>
        <p>The keystrokes are:</p>
        <dl>
          <dt>f</dt>
          <dd>feminines</dd>
          <dt>m</dt>
          <dd>masculines</dd>
          <dt>n</dt>
          <dd>Neuters</dd>
          <dt>x</dt>
          <dd>next xxx string</dd>
        </dl>
      </section>
      
    </section>
  </body>
</document>
