<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE document PUBLIC "-//APACHE//DTD Documentation V2.0//EN"
"http://forrest.apache.org/dtd/document-v20.dtd">
<document xml:lang="en">
  <header>
    <title>Howto setup emacs</title>

    <authors>
      <person email="borregaup@samediggi.no" name="Børre Gaup" />
    </authors>
  </header>

  <body>
    <section>
      <title>Introduction</title>

      <p>Emacs has to be setup to be use UTF-8 as its default decoding and to
      use nxml mode when writing xml-documents.</p>
    </section>

    <section>
      <title>Fetching and unpacking nxml-mode</title>

      <p><a href="http://thaiopensource.com/download">Fetch nxml mode</a>. You
      end up with a folder called nxml-mode-2xxxxxxx. Move it to the Documents
      folder.</p>
    </section>

    <section>
      <title>Setting up</title>

      <p>After this is done, open the file $HOME/.emacs. Add the following
      lines to that file.</p>

      <source>
;Make nxml-mode available to emacs
(load "~/Documents/nxml-mode-20041004/rng-auto.el")

; Load nxml-mode automatically for files with the following endings
(add-to-list 'auto-mode-alist
(cons (concat "\\." (regexp-opt '("xml" "xsd" "sch" "rng" "xslt" "svg" "rss") t) "\\'")
'nxml-mode))

; Make Emacs UTF-8 compatible for both display and editing:
(prefer-coding-system 'utf-8)
(set-terminal-coding-system 'utf-8)
(set-keyboard-coding-system 'utf-8)

; Turn on syntax colouring in all modes supporting it:
(global-font-lock-mode t)
      </source>

      <p>Save and close the file.</p>

      <p class="last_modified">Last modified $Date$, by $Author$</p>
    </section>
  </body>
</document>