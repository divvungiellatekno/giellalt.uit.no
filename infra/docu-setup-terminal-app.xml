<?xml version = '1.0' encoding = 'UTF-8'?>
<!DOCTYPE document PUBLIC "-//APACHE//DTD Documentation V2.0//EN" "http://forrest.apache.org/dtd/document-v20.dtd">
<document xml:lang="en">
  <header>
    <title>Setup of Terminal.app</title>
    <authors>
      <person email="sjurnm@mac.com" name="Sjur Moshagen" />
    </authors>
  </header>
  <body>
    <section>
      <title>Terminal.app</title>
      <p>Now that the shell can handle 8-bit IO, we need to define how we want
      the terminal application to interpret those 8 bits. The following setup
      is made for Apples Terminal.app (in /Applications/Utilities/), similar
      steps will need to be made for other terminal applications.</p>

      <p>Type Cmnd-I</p>
      <ul>
    <li>in the pane 'Monitor' or '&lt;unknown monitor&gt;', select UTF-8
    as the character encoding (towards the bottom)</li>
    <li>click the button at the bottom named 'Bruk innstillinger som
    standard'/'Use preferences as default'</li>
    <li>in the pane 'Emulation' ('Emulering'), uncheck 'Escape-koder for
    ikke-ASCII-tegn'</li>
    <li>in the pane 'Emulation' ('Emulering'), uncheck 'Escape-koder for
    ikke-ASCII-tegn'</li>
    <li>click the button at the bottom named 'Bruk innstillinger som
    standard'/'Use preferences as default' again</li>
    <li>to be able to input accented characters, or characters requiring
    the option key, make sure that the preference 'Bruk tilvalgstast som
    metatast'/'Use Option key as Meta key' is not checked in the page
    'Tastatur'/'Keyboard'</li>
      </ul>
      <p class="last_modified">Last modified: $Date$, by $Author$</p>
    </section>
  </body>
</document>
