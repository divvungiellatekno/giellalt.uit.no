Short version:

* make Team Agent create one using XCode
* make Team Agent export the private key from the local key chain
** use a strong password
* send enkrypted private key to team member(s)
* import the certificate into team member key chain
** decrypt using the same strong password
* get info on the certificate, copy name of public part of certificate
* use the name when signing installation packages
